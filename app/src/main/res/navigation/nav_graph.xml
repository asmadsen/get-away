<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/nav_graph"
            app:startDestination="@id/landing">

    <fragment android:id="@+id/landing" android:name="no.asmadsen.getaway.Landing" android:label="landing_fragment"
              tools:layout="@layout/landing_fragment">
        <action android:id="@+id/action_landing_to_map" app:destination="@id/map"/>
        <action
            android:id="@+id/action_landing_to_chooseAirport"
            app:destination="@+id/chooseAirport" />
    </fragment>
    <fragment android:id="@+id/map" android:name="no.asmadsen.getaway.Map" android:label="map_fragment"
              tools:layout="@layout/map_fragment"/>
    <fragment
        android:id="@+id/chooseAirport"
        android:name="no.asmadsen.getaway.ChooseAirport"
        android:label="ChooseAirport" />
</navigation>